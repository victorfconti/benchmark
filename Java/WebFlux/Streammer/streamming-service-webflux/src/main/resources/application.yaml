server:
  port: 8081

spring:
  application:
    name: streammer-full-hd
  servlet:
    multipart:
      max-file-size: 10024KB
      max-request-size: 10024KB

video:
  path: ${VIDEO_PATH:/static/video.mp4}